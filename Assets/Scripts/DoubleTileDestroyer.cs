using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoubleTileDestroyer : MonoBehaviour
{
    public ObjectList SelectedTiles;

    //Update is called once per frame

    //void Update()
    //{
    //    Debug.Log(SelectedTiles.Stack.Count);

    //    if (SelectedTiles.Stack.Count > 1)
    //    {
    //        var newStack = SelectedTiles.Stack;
    //        var lastItem = newStack.Pop();
    //        Debug.Log(lastItem);

    //        if (newStack.Contains(lastItem))
    //        {
    //            SelectedTiles.Stack.Pop();
    //            SelectedTiles.Stack.Pop();
    //        }
    //    }
    //}
}
